import {ArrowUpRight,Coins,Sparkles} from 'lucide-react';
export default function BalanceCard({balance,onWithdraw}:{balance:string;onWithdraw:()=>void}){return <section className="balance-card"><div className="glow g1"/><div className="glow g2"/><div className="balance-top"><span className="eyebrow"><Coins size={14}/> AVAILABLE BALANCE</span><span className="live"><i/> LIVE</span></div><div className="balance-value">{Number(balance).toLocaleString(undefined,{maximumFractionDigits:6})}<span> coins</span></div><div className="balance-foot"><span><Sparkles size={15}/> Keep earning to grow your balance</span><button onClick={onWithdraw}>Withdraw <ArrowUpRight size={15}/></button></div></section>}
