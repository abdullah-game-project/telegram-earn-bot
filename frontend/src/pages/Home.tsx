import {ArrowRight,CalendarCheck,CheckCircle2,Gift,Send,Users,Zap} from 'lucide-react';
import BalanceCard from '../components/BalanceCard'; import Section from '../components/Section'; import type {Tx,User} from '../types';
const tasks=[['Daily check-in','Open the app every day','+25',CalendarCheck],['Join & explore','Discover a new earning task','+50',Send],['Invite a friend','Bring your first referral','+100',Users]] as const;
export default function Home({user,balance,tx,onWithdraw,onEarn,onFriends}:{user:User;balance:string;tx:Tx[];onWithdraw:()=>void;onEarn:()=>void;onFriends:()=>void}){const name=user.first_name||user.username||'there';return <div className="page"><header className="top"><div><div className="muted">Welcome back</div><h1>Hey, {name} <span>👋</span></h1></div><div className="avatar">{(name[0]||'U').toUpperCase()}</div></header><BalanceCard balance={balance} onWithdraw={onWithdraw}/><div className="quick-grid"><button onClick={onEarn}><span className="quick-icon purple"><Zap/></span><b>Earn now</b><small>Find rewards <ArrowRight size={13}/></small></button><button onClick={onFriends}><span className="quick-icon blue"><Users/></span><b>Invite friends</b><small>Earn together <ArrowRight size={13}/></small></button></div><Section title="Today's opportunities" action={<button className="link" onClick={onEarn}>View all</button>}><div className="task-list">{tasks.map(([title,desc,reward,Icon],i)=><button className="task" key={title} onClick={onEarn}><span className="task-icon"><Icon size={19}/></span><span className="task-copy"><b>{title}</b><small>{desc}</small></span><strong>{reward}</strong><ArrowRight size={17}/></button>)}</div></Section><Section title="Recent activity"><div className="activity-list">{tx.slice(0,4).map(t=><div className="activity" key={t.id}><span className={'activity-icon '+(t.type==='withdraw'?'red':'green')}>{t.type==='withdraw'?'↑':'+'}</span><span><b>{t.description||t.type}</b><small>{new Date(t.created_at).toLocaleString()}</small></span><strong className={t.type==='withdraw'?'negative':'positive'}>{t.type==='withdraw'?'−':'+'}{Math.abs(Number(t.amount))}</strong></div>)}{!tx.length&&<div className="empty"><Gift size={22}/><span>No activity yet. Your first reward will show here.</span></div>}</div></Section></div>}
