import {ArrowDownLeft,ArrowUpRight,Clock3,WalletCards} from 'lucide-react'; import type {Withdrawal} from '../types';
export default function Wallet({balance,items,onWithdraw}:{balance:string;items:Withdrawal[];onWithdraw:()=>void}){return <div className="page"><div className="page-title"><div><div className="muted">YOUR MONEY</div><h1>Wallet 💸</h1></div></div><div className="wallet-card"><span>Current balance</span><b>{Number(balance).toLocaleString()}</b><small>coins available</small><button className="primary" onClick={onWithdraw}><ArrowUpRight size={17}/> Withdraw</button></div><div className="section-head"><h2>Withdrawal history</h2></div><div className="activity-list">{items.map(w=><div className="activity" key={w.id}><span className="activity-icon red"><ArrowUpRight size={17}/></span><span><b>{w.payout_method} · {w.status}</b><small>{new Date(w.requested_at).toLocaleString()}</small></span><strong className="negative">−{Number(w.amount).toLocaleString()}</strong></div>)}{!items.length&&<div className="empty"><Clock3 size={22}/><span>No withdrawals yet.</span></div>}</div></div>}
