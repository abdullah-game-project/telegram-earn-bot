import {Crown,Medal,Trophy} from 'lucide-react'; import type {Leader} from '../types';
export default function Leaderboard({payouts,refs}:{payouts:Leader[];refs:Leader[]}){const Board=({title,data,unit}:{title:string;data:Leader[];unit:string})=><div className="board"><div className="section-head"><h2>{title}</h2><Trophy size={18}/></div>{data.slice(0,10).map((x,i)=><div className={'rank '+(i<3?'top-rank':'')} key={x.telegram_id}><span className="rank-no">{i===0?<Crown size={17}/>:i===1?<Medal size={17}/>:i+1}</span><span className="rank-avatar">{(x.display_name||'U')[0]}</span><span className="rank-name"><b>{x.display_name}</b><small>{x.username?`@${x.username}`:'Telegram user'}</small></span><strong>{Number(x.value).toLocaleString()} {unit}</strong></div>)}{!data.length&&<div className="empty">Leaderboard is waiting for its first entries.</div>}</div>;return <div className="page"><div className="page-title"><div><div className="muted">COMMUNITY</div><h1>Leaderboard 🏆</h1></div></div><Board title="Top payouts" data={payouts} unit="coins"/><Board title="Top referrers" data={refs} unit="friends"/></div>}
